# Screenshots

Add the two required screenshots here, then re-run `python make_report_pdf.py`
to embed them in `module_6_report.pdf`:

- `website.png` — the running app at http://localhost:8080
- `rabbitmq.png` — the RabbitMQ management UI at http://localhost:15672
  (show the `tasks_q` queue, ideally mid-processing)

Capture them after `docker compose up --build` is running and you have clicked
**Pull Data** and **Update Analysis**.
