# Documentation

The submission report — architecture, service roles, RabbitMQ message flow,
database initialisation, worker behaviour, and verification steps — is generated
as `../module_6_report.pdf` by `../make_report_pdf.py`.

Per the assignment, the Sphinx API documentation is **not** required to be
updated for Module 6, so it is intentionally not carried forward here. The
narrative architecture docs live in the top-level `README.md` and the report PDF.
