flask==3.1.1
psycopg[binary]==3.3.4
beautifulsoup4==4.12.3
selenium==4.27.1
lxml==5.3.0
webdriver-manager==4.0.2

# Report generation. make_limitations_pdf.py imports fpdf but this was never
# declared, so limitations.pdf could not be rebuilt from a clean checkout.
fpdf2>=2.7
