# Secrets / local config — never commit AWS keys or .env
.env
*credentials*.csv

# Local Python env / caches
venv/
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# The raw dataset pulled down from S3 at runtime (regenerated by the notebook;
# not a required deliverable). The CLEANED dataset (cleaned_gradcafe.json) IS a
# required deliverable and is committed.
applicant_data_SM.json

# OS cruft
.DS_Store
